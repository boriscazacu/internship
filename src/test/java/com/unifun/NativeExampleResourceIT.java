package com.unifun;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeExampleResourceIT extends GroupEndpointTest {

    // Execute the same tests but in native mode.
}